# GitHub update – 0.3.0-dev

This is a full replacement package for the Render DEV repository.

Recommended commit message:

```text
feat: add fail-closed BFH case evidence gate
```

After push, confirm Render health reports `0.3.0-dev`, then refresh the MCP connection in Copilot
Studio and run the direct `get_case` gate test before changing the agent skill.
